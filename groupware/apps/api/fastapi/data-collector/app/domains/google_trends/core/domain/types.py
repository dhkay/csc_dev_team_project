"""도메인 타입: 소스 식별자와 수집 지역, 보관 수.

구글 트렌드 일별 인기 검색어(RSS)는 지역 하나당 10건을 한 번에 준다. 기간 개념이 없다
(그 시점의 인기 목록이다).
"""

from __future__ import annotations

# 제품 수준 소스 식별자. AI 도구의 소스 설정 key 와 카탈로그가 같은 문자열을 쓴다.
SOURCE_ID = "GOOGLE_TRENDS"

# 수집 가능한 지역(ISO 3166-1 alpha-2). 목록의 주인은 이 소스이고 카탈로그가 그대로 내보낸다.
GEOS: tuple[tuple[str, str], ...] = (
    ("KR", "대한민국"),
    ("US", "미국"),
    ("JP", "일본"),
)

# 보관 수 = 피드가 주는 개수. KR/US/JP 모두 10건이었다(실측). 진행률의 분모이기도 하므로
#   실제보다 크게 잡으면 완료된 수집이 영원히 절반쯤에서 멈춘 것처럼 보인다.
KEEP_COUNT = 10
