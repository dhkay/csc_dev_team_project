"""도메인 타입: 소스 식별자, 보관 수, 입력 제약.

SerpApi 를 거쳐 구글 검색 결과 첫 페이지를 가져온다. 분야도 기간도 없고 입력은 검색어 하나다.

쿼터가 좁은 소스다. 요금제가 월 검색 횟수를 정하고(무료 100회) 한 번의 수집이 그 한 번을
쓴다. 그래서 이 소스는 크론 자동 재수집을 끄고 조회가 있을 때만 모은다(module.py 참고).
"""

from __future__ import annotations

# 제품 수준 소스 식별자. AI 도구의 소스 설정 key 와 카탈로그가 같은 문자열을 쓴다.
SOURCE_ID = "GOOGLE_SEARCH"

# 보관 수. 첫 페이지가 10건 안팎이다. 진행률의 분모이기도 하다.
KEEP_COUNT = 10

# 검색어 최대 길이. 검색어가 곧 타깃 키라 길이를 열어 두면 키가 무한정 길어진다.
KEYWORD_MAX_LENGTH = 100
