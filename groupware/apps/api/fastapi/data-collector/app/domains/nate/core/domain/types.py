"""도메인 타입: 소스 식별자와 보관 수.

네이트 실시간 검색어는 그 순간의 목록 하나다. 분야도 기간도 없다(타깃이 하나뿐인 소스다).
"""

from __future__ import annotations

# 제품 수준 소스 식별자. AI 도구의 소스 설정 key 와 카탈로그가 같은 문자열을 쓴다.
SOURCE_ID = "NATE_REALTIME"

# 타깃 키. 고를 것이 없는 소스라 상수 하나로 고정한다(빈 문자열을 키로 쓰면 저장된 행을
#   눈으로 봤을 때 그게 무엇인지 알 수 없다).
TARGET_KEY = "realtime"

# 보관 수. 실측에서 10건이 온다. 진행률의 분모이기도 하다.
KEEP_COUNT = 10
