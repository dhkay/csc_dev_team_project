"""도메인 타입: 소스 식별자, 보관 수, 입력 제약.

검색어로 영상을 찾고(`search.list`) 그 영상들의 통계를 채운다(`videos.list`). 두 호출이 한 수집을
이루므로 소스도 하나다. 나눠 두면 소비자가 두 번 조회해 조합해야 한다.

쿼터가 좁은 소스다. `search.list` 는 호출당 100 유닛이고 하루 한도가 10,000 이라 하루 100회
남짓이 전부다(`videos.list` 는 1 유닛). 그래서 이 소스는 크론 자동 재수집을 끄고 조회가 있을
때만 모은다(module.py 참고).
"""

from __future__ import annotations

# 제품 수준 소스 식별자. AI 도구의 소스 설정 key 와 카탈로그가 같은 문자열을 쓴다.
SOURCE_ID = "YOUTUBE_VIDEO"

# 보관 수 = 한 번에 찾는 영상 수. 50 까지는 검색 호출이 한 번이라 쿼터가 같다(그 위는 페이지가
#   갈려 호출이 늘고, 100 유닛이 또 나간다). 진행률의 분모이기도 하다.
KEEP_COUNT = 10

# 검색어 최대 길이. 검색어가 곧 타깃 키라 길이를 열어 두면 키가 무한정 길어진다.
KEYWORD_MAX_LENGTH = 100
